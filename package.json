{
  "name": "@santima10/test",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/**"
    ],
    "nohoist": [
      "**/firebase-admin",
      "**/firebase-admin/**",
      "**/firebase-functions",
      "**/firebase-functions/**"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "publish:registry": "lerna run publish:registry",
    "deploy": "lerna run deploy"
  },
  "devDependencies": {
    "lerna": "3.16.2"
  }
}
